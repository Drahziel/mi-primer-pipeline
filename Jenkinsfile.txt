pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Clonando el código...'
            }
        }
        stage('Build') {
            steps {
                echo 'Compilando proyecto desde webhook...'
            }
        }
        stage('Test') {
            steps {
                echo 'Ejecutando tests...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Desplegando aplicación...'
            }
        }
    }
}
